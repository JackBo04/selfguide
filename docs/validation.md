# v0.5.0 文本通道验证

## 发送确认与等待回执（2026-09-25）

修正用户气泡和 `Thinking` 占位被误判为发送成功的问题。出现本轮助手实际内容后返回 `sent:true`；只有本地气泡时返回 `submitted:true, sent:false, confirmation:ui_only`，沿用 `send_pending` 收取同一轮，完整回复到达后再登记发送与回复。新版地址映射保留该轮窗口绑定，消息消失时报告轮次不匹配，不采集旧回复。

等待记录增加最近网页回执的时间与原因，以及状态写入时间，避免把当前 probe 尚未返回误解为网页一直在生成。受控测试覆盖占位状态、消息消失、未确认发送后的完整收取及并行任务；25 项 Python 回归测试通过。实站本轮当前只显示上一轮内容，并出现会话准备请求 HTTP 403；未取得本轮完整稿件，未修改论文。

## 新版发送按钮与网站拒绝诊断（2026-09-19）

适配表单中的 `Send` 提交按钮及 `data-selected-reasoning-effort=xhigh` 档位。网页返回 `Unknown error` 时立即报告发送未确认；带 `local-chatgpt%3A` 的临时地址不作为发送成功依据。受控测试覆盖新版按钮收发、网站拒绝后不重复点击；原有扩展收发测试通过。

实站此次确认按钮已触发请求，但网站对会话初始化等请求返回 HTTP 403，并带 `cf-mitigated: challenge`。主页和正常登录入口均未显示验证框，未取得服务器会话或方案回复。此处记录为外部阻塞，不能据此宣称真实收发已恢复。

## 新版项目入口兼容（2026-09-19）

新版网页会把项目入口重定向到 `/`、会话重定向到 `/c/<id>`。扩展在自己创建的任务窗口中按项目选择器的精确项目 ID 选择项目，保留可恢复的项目地址，并在结果的 `page_url` 中报告实际地址。项目切换和其他会话地址仍会阻止发送。

`node tests/test_modern_project.mjs` 在受控 Chromium 中覆盖项目选择、DOM 收发、地址映射、项目切换拦截、关闭及原会话恢复。24 项 Python 测试、server/local 多窗口回归和接收器恢复测试通过。真实服务器复用原登录后已确认原任务窗口及输入框可用；此次未发送真实任务消息，不能将模拟收发测试视作新版实站完整闭环验收。

## 2026-09-16 窗口回收与原会话续聊

任务登记 complete 后自动排队清理自己的标签页；`cleanup_windows.py` 可预览、清理已完成任务的积压窗口。网页存在草稿、附件、生成中的回复，或最新消息不属于本任务时保留。手动添加的其他标签页不删除；并发清理仍保留最后一个浏览器窗口。清理失败不撤销本地完成记录。

已完成任务可通过 `session.py resume` 恢复执行，再以 `open --restore` 打开保存的会话地址。轮次继续累加；未确认的关闭请求须先结束，防止它随后关闭已恢复的窗口。

24 项 Python 测试及服务器／本地两版多窗口回归通过，覆盖完成后自动关闭、草稿／生成保护、错消息拒绝、保留手动标签页、恢复原 URL 后发送下一轮，以及同时关闭最后两个窗口时保留一个。真实服务器合成任务通过关闭、恢复原对话、核对两轮历史与原回复、再次完成后自动关闭；没有新增网页消息或截图。旧对话的历史数据晚于输入框出现，验证在历史加载完成后进行。

## 2026-09-16 调用故障修复

21 项 Python 测试通过；服务器版与本地版扩展回归、多窗口隔离测试通过。新增检查覆盖：跨任务标记在入队前拒绝、当前交接文件的哈希与会话地址校验、已完成任务不能继续发送、反引号转为换行（含其后空格被移除）的正文匹配，以及延迟出现的输入框。正文实质变化与错任务／错轮次仍被拒绝。

`node tests/test_page_reconnect.mjs` 验证扩展仅在明确没有消息接收者时重新注入内容脚本；响应端口中断等不确定结果不会重试命令。真实服务器更新后，原任务窗口通过新版扩展重新读取，无需刷新该页面或重新登录。无输入框时可读取诊断快照；网页重试页有独立错误码。

真实网页首轮暴露了反引号后空格被移除的额外差异，修复后读取已有回复并恢复记录，没有重发。最终第二轮的 compose → send → wait_reply → reply 首次执行通过，收到预期 `REPAIR_OK_2` 与 `42`，状态登记为 complete，收发过程零截图。初始空白测试页需要单独刷新；故障定位用过一次截图，因此不把整次排查计为零截图。此次未在用户本地电脑实测。

两个版本的 `SKILL.md` 入口未增加文本；恢复说明按需读取。受控测试的普通状态返回 234 字节，完整诊断快照约 50 KB；这不是 API 账单或 token 用量测量。

文本通道从 `experimental/dom-text-bridge` 开发并经过合并前验收，作为 v0.5.0 的默认流程。服务器升级复用原已登录浏览器，安装器保留旧版备份。

## 2026-09-13 合并前验收

19 项 Python 回归通过；服务器版、本地版分别通过受控 Chromium 收发与多窗口测试。附件卡片、发送就绪、等待恢复、任务隔离、同名附件内容隔离与原草稿保护均纳入检查。

真实附件测试发现并修复了四处页面适配差异：没有文本的附件图标；网页使用 `aria-disabled` 表示发送未就绪；项目将同名附件改成 `name(2).ext`；已发送消息折叠连续空段落。发送前仍逐字核对原草稿；仅在已发送消息的匹配中兼容空段落显示。发送按钮就绪前等待，点击后结果不明不会自动重试。附件确认匹配新出现的卡片，保存网页实际显示名称。

合成 TXT 中的三项数值及附件独有随机码由网页读出；Codex 按指导执行 Python、独立核对 JSON，再上传同一会话，网页返回 `SELFGUIDE_ACCEPTED`。适配期间按原任务记录恢复，诊断用过一次截图，不能将整个升级过程称为零截图。随后另建独立任务，真实后台附件上传、首次发送、完整回复读取与验收直接通过；该轮没有上传或发送重试、焦点切换或截图，页面初始化的只读检查允许继续等待。

README 由网页起草并按具体反馈修订，Codex 保存和检查。上述实站测试使用服务器 Chrome；本地用户电脑仍未实测。修复前的失败及恢复记录保存在私有测试工作区，不放入源码或安装包。

真实 Chromium 扩展 + HTTP 桥接 + Python 等待程序在受控网页完成以下检查：普通状态不带历史正文；附件卡片确认；填入与发送确认；自动等待；超时后恢复原读取任务；保存完整原文；拒绝旧轮次、只有代码复制按钮的未完成回复和仍在变化的回复；登录页面返回诊断错误。完整任务记录的 prepare → sent → DOM 回复验收 → complete 也已纳入联调，含多行交接消息。测试流程没有调用截图。服务器版和本地版均使用独立临时配置与 profile。

合成大回复测试中，常规状态为 226 字节，显式全文诊断为 50,715 字节。这说明传回模型的状态可以缩小，不代表实际 API 账单或订阅额度已按同一比例下降。测试没有调用模型，也没有测量真实账户额度。

Python 检查覆盖既有收发恢复、配对角色、配置与安装升级，以及 watcher 不泄露未完成正文、只发读取命令、超时不要求截图、输出防覆盖、恢复时核对任务和原文。旧的真实网页登录／附件验证属于稳定版本历史，不作为这个新通道的真实网站验收。

已在服务器的已登录 Chrome 加载并配对扩展；尚未在用户本地电脑实测。真实页面的段落编辑器和回复容器适配已加入回归测试；附件名称和档位控件仍可能需要按实际页面适配。本分支不承诺风控豁免，不自动处理人机验证。

## 真实服务器适配

首次加载时 Chrome 的目录选择框直接返回取消，改用原生目录拖放完成扩展加载与配对，未重启或退出原已登录浏览器。初次设置与异常诊断使用过截图，不能将整次安装计为零截图。

实站暴露并修复两处差异：段落式编辑器的 `innerText` 会增加视觉空行，现按段落与显式换行核对，仍拒绝实质不同的草稿；当前回复使用 section／agent-turn 容器，现把回复级复制按钮限定在对应回复容器内。两个版本的受控浏览器回归均通过，保留对未完成回复、旧轮次和代码块复制按钮的拒绝检查。

2026-09-11 在真实服务器账户的 selfguide 项目完成两轮合成测试：网页指导 Python 计算 `17+25`，Codex 实际输出 `42` 并反馈，网页返回 `SELFGUIDE_ACCEPTED`。两轮回复都经 DOM 保存原文并通过任务／轮次校验，任务已记录 complete。适配完成后的第二轮 compose → send → 程序等待 → reply 全程无截图。此验收不覆盖用户电脑、本次真实附件上传或自动切档。

## 多窗口隔离验证

新增测试覆盖任务队列隔离、单任务未确认操作仍受保护、并行任务不互相阻塞，以及 watcher 恢复时不能换任务 ID。受控浏览器测试验证两路独立窗口同时收发，分别上传同名但内容不同的附件；较快任务在另一路仍生成时完成，回复只含各自附件内容。原绑定窗口的草稿保留。

服务器版与本地版均通过受控多窗口测试：两次确认响应丢失后能从独立结果记录恢复；窗口关闭／旧绑定失效不影响另一任务，显式恢复沿用保存的会话 URL。旧单标签页流程也保留回归检查。

首次实站并发时，B 独立完成，A 的被遮挡窗口只渲染了部分回复；聚焦 A 后取得原完整回复，没有重发消息。加入仅对任务窗口的自动平铺后，两路回复均完整渲染；窄窗口的折叠按钮文字造成发送核对误报，现限定读取消息正文容器，已加入回归测试。

2026-09-11 最终实站复测：两个独立任务窗口并发完成 compose → send → wait_reply → DOM 回复登记 → complete，分别核对计算结果和随机校验串，没有串入另一任务内容。该轮全程零截图、零窗口焦点切换，没有重发或人工修正发送状态。服务器原登录保持；本地电脑仍未实测，也不保证窗口被最小化或完全遮挡时仍能刷新。

两个版本另通过折叠消息正文核对、关闭所有测试聊天页后仍能及时新开任务的检查。浏览器扩展自行短轮询，模型不参与轮询；扩展唤醒后恢复计时，暂停时停止领取新命令。

## 上下文精简验证

相对 `30e560b`，服务器／本地版常规必读文本由 8,497 字符降为 2,486／2,488；写作另读 243 字符，其余参考按需加载。同一示例任务号的每轮交接提示由 352 降为 194 字符。字符数不是 token 用量或额度测量。

19 项 Python 测试通过。新增恢复输出测试为两个版本分别加入 1,000 条历史事件和长格式核对说明，确认 `status --brief` 输出不随历史增长、保留当前收发路径和恢复状态，原始记录不变，原完整 `status` 接口仍可读取历史。原有任务号／轮次检查、暂停恢复和收发状态测试继续通过。

### 2026-09-13 指令审查

依据 [OpenAI 的 skills 与 prompts 审查建议](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)，检查触发范围、按需读取、固定步骤和完成边界。本次只修订指令与文档：

- 区分网页协作与维护 skill 本身，减少无关任务触发。
- 明确 Codex 连续实施一个阶段，再向网页反馈新增结果或方向性疑问；以用户目标和网页验收作为完成条件。
- 将附件操作移到按需参考；移除通用入口中的实验安装限制，发布策略仍由仓库说明与用户要求决定。
- 已确认且未改变的网页档位沿用记录；切档工具缺失时先推进可做的工作，确实阻塞再请用户介入。

相比 `ad2760e`，常规首次读取从 2,742／2,747 字符降至 2,486／2,488，约再减 9%。写作分工、空置模块、任务窗口隔离、发送确认和完整回复校验保留。两个版本的结构校验和相对链接检查通过；未改运行脚本，未重跑真实网页任务，也未测量行为提升或账单变化。

后续优先评估把每轮 prepare／compose／send／wait／reply 封装为可恢复的高层命令，减少模型逐条组织调用。需用多轮任务比较成功率、重复发送、模型参与次数及上下文用量；在保留现有恢复与隔离语义前，不将其计为已实现的优化。

---

## 稳定版本历史记录


v0.4.0 将写作归入模块结构；绘图和实验迭代只保留空位，没有实现专用流程。安装器新增相同内容复用、备份升级、符号链接目标更新及原 `$selfguide` 名称保留。测试覆盖重复安装、更新后的备份与额外本地文件保留；两个包的安装和升级均在临时目录检查。本次未新增网页写作或用户电脑的真实扩展验收。

v0.3.0 统一项目、skill、目录、配置和扩展内部名称，同时新增正文由网页版起草与实质改写的分工规则，并将写作要求加入每轮交接提示。本轮没有额外提交真实写作任务，规则检查不等于正文质量验收。真实网页已将原项目重命名为 `selfguide`，保留项目 ID 和会话；当前部署迁移为 `$selfguide` 与 `selfguide-workspace/`，旧路径保留兼容别名，登录进程未重启。新路径和旧环境变量回退、同一会话 URL slug 变化，以及发行包安装均有自动检查。本地扩展仍仅在受控页面联调，未在用户电脑验收。下方旧版本记录保留当时名称。

v0.2.1 加入可复制文本交接要求、任务／轮次标记检查和来源记录。两个版本的测试覆盖错任务、错轮次、截断、来源声明、原文保存、格式例外留痕和旧任务恢复。本地扩展在模拟页面验证交接代码块原文提取，保留完整回复供核对，避免混入代码块工具栏文字。真实服务器页面另通过回复复制按钮取得完整的 4,321 字符、134 行原文；本次未另发真实网页轮次验证模型遵守新格式，也未在用户电脑验证本地扩展。

v0.2.0 将开源项目品牌及仓库更名为 SelfGuide，更新 README 视觉、安装链接、扩展显示名和安装包名称。现有 skill 调用名、ChatGPT 项目与运行数据位置继续兼容；本次更名不代表新增自我训练能力或本地自动切档能力。

v0.1.2 同步网页版主导、Codex 执行与网页版 xhigh／Pro 档位规则。当前服务器页面通过截图确认显示 Extra High；没有在本次规则更新中实际运行 Pro 难题，也没有新增本地扩展的自动切档能力。

v0.1.1 更新 README 和 Agent 安装规范，并重新打包。浏览器扩展、桥接和桌面执行代码沿用 v0.1.0；下面的真实验证范围保持不变。本次另检查说明中的本地链接，以及两个新版 ZIP 的解压与安装。

验证日期：2026-09-10。没有把浏览器 Cookie、账号配置、真实聊天内容、截图或研究文件打入发行包。

## 服务器浏览器版

原部署已在用户自己的真实 astra 项目做过两轮闭环：TXT 附件包含仅存在于文件中的随机码；网页版读出该码及合成数据并指导生成 JSON；Codex 独立验证后将 JSON 结果作为附件回传；网页版给出验收通过。全过程复用了现有普通 Chrome 登录。

发行包沿用该版本的桌面收发与任务状态逻辑，并移除了用户固定路径及私有项目链接。服务启动代码整理为普通 Chrome 模式，支持系统依赖与用户目录依赖。新打包的服务使用临时空白浏览器、独立显示号和端口做启动检查，不触碰原已登录浏览器。

## 本地浏览器版

已运行真实 Chromium 扩展环境，加载发行包的 service worker 和内容脚本，通过真实 HTTP 桥接连接到受控模拟页面，验证：

- 只读取任务页面最近消息，不收集模拟侧栏历史。
- 附件字节和 SHA-256 校验一致，文件进入网页上传输入。
- 填入草稿后核对文字，再提交，确认本轮消息和会话 URL。
- 读取本轮完整回复，拒绝将之前的回复当成本轮结果。
- 回复后回到项目页，准备下一任务。

**该测试使用模拟页面，未登录真实 ChatGPT；尚未在用户本地电脑运行。** v0.1.0 的本地版作为初版提供，首次安装需在实际 ChatGPT 页面完成合成附件的两轮验收。网页版 DOM 变化可能需要维护 `extension/content.js`；本地系统、浏览器后台节流、SSH 可达性和网站登录状态仍需现场确认。

## 自动检查

Python 测试覆盖桥接鉴权、角色隔离、拒绝外部网页 Origin、同一命令只领取一次、结果确认幂等、未确认操作恢复、过期命令、项目边界、附件完整性，以及两个 skill 的任务目录、附件来源、禁止覆盖、发送状态和暂停恢复。

两个 skill 通过 skill-creator 的结构检查；Python 与 JavaScript 通过语法检查。发行包采用明确目录清单生成，解压后在临时目录测试安装，避免依赖当前工作区的绝对路径。

## 边界

没有承诺永不重新登录或绕过人机验证。本地扩展不会自己启动 Codex，本地电脑、浏览器和 SSH 隧道需要保持运行。服务器版新环境需要系统依赖；现有服务器浏览器保持登录不等于所有新服务器均已验证。桥接默认单文件 8 MiB 不是网站官方限制。
